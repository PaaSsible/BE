replicaCount: 1

image:
  repository: nginx
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources: {}

env:
  - name: SPRING_PROFILES_ACTIVE
    value: prod

envFrom:
  - secretRef:
      name: common-secret
